链表（Linked list）是一种常见的基础数据结构，
是一种线性表，但是并不会按线性的顺序存储数据，
而是在每一个节点里存到下一个节点的地址。

LinkedList：基于双向链表实现，不支持快速随机访问。
添加和删除操作速度较快，因为只需要改变节点的指针。



1.以下情况使用 ArrayList :
频繁访问列表中的某一个元素。
只需要在列表末尾进行添加和删除元素操作。

2.以下情况使用 LinkedList :
不过，LinkList主要用于队列开发
或者也可以用于栈的设计

你需要通过循环迭代来访问列表中的某些元素。
需要频繁的在列表开头、中间、末尾等位置进行添加和删除元素操作。


