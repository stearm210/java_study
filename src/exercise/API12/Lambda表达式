Lambda主要是用于简化匿名内部类代码的写法
格式：
(被重写方法的形参列表)->{
   被重写方法的方法体代码
}

Lambda表达式只能简化函数式接口的匿名内部类
:
什么是函数式接口？
1.有且仅有一个抽象方法的接口
2.使用到的函数式接口都会包含一个@Functionallnterface
的注解，拥有这个注解的接口一定是函数式接口


Lambda表达式省略规则
1.参数类型可以省略不写
2.如果只有一个参数，参数类型可以省略，同时括号可以省略
3.如果Lambda表达式中的方法只有一行代码，可以省略大括号
不用写，如果需要省略的部分是return语句的缩写，则return可以不写


